<div class="auth-container">
    <h2>Забыли пароль?</h2>
    <p>Введите email, и мы отправим ссылку для сброса пароля.</p>

    {{#if error}}
        <div class="alert alert-error">{{error}}</div>
    {{/if}}
    {{#if success}}
        <div class="alert alert-success">{{success}}</div>
    {{/if}}

    <form action="/auth/forgot-password" method="POST" class="auth-form">
        <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required>
        </div>

        <button type="submit" class="btn btn-primary btn-full-width">Отправить ссылку</button>
    </form>
    <p class="auth-footer-link">
        <a href="/auth/login">Вспомнили? Войти</a>
    </p>
</div>