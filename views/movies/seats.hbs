<script src="https://cdn.tailwindcss.com"></script>
<script>
    // Настройка Tailwind CSS для использования шрифта Inter (предполагаем, что он используется в других частях приложения)
    tailwind.config = {
        theme: {
            extend: {
                fontFamily: {
                    sans: ['Inter', 'sans-serif'],
                },
                colors: {
                    'primary-red': '#D12229', // Основной красный цвет (как в CinemaBox)
                    'dark-bg': '#1E1E1E', // Основной темный фон
                    'dark-card': '#252525', // Фон карточек и панелей
                    'text-light': '#F0F0F0',
                    'text-muted': '#A0A0A0',
                }
            }
        }
    }
</script>

<style>
    /* Базовые стили для страницы выбора мест */
    .seat-selection-page {
        background-color: var(--bg-primary);
        color: var(--text-primary);
        min-height: calc(100vh - 150px);
        font-family: 'Inter', sans-serif;
        padding: 2rem 1rem;
        transition: background-color 0.3s ease, color 0.3s ease;
    }

    .page-container {
        max-width: 1400px;
        margin: 0 auto;
    }

    /* Заголовок и навигация */
    .seat-selection-header {
        text-align: center;
        margin-bottom: 3rem;
    }

    .back-link {
        color: var(--accent-primary);
        text-decoration: none;
        font-weight: 500;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 1rem;
        transition: color 0.2s;
    }

    .back-link:hover {
        color: var(--accent-hover);
        text-decoration: underline;
    }

    #movie-title {
        font-size: 2.5rem;
        color: var(--text-primary);
        margin-bottom: 1rem;
        font-weight: bold;
        line-height: 1.2;
    }

    /* Выбор времени сеанса */
    .time-selection-container {
        background-color: var(--bg-card);
        padding: 1.5rem;
        border-radius: 12px;
        box-shadow: 0 4px 20px var(--shadow-color);
        max-width: 600px;
        margin: 0 auto 2rem;
        position: relative;
        transition: all 0.3s ease;
    }

    .time-selection-container h2 {
        font-size: 1.25rem;
        color: var(--text-primary);
        margin-bottom: 1rem;
        font-weight: 600;
        text-align: center;
    }

    .time-button-group {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        width: 100%;
        align-items: center;
    }

    .time-button {
        padding: 0.75rem 1.5rem;
        border-radius: 8px;
        font-weight: 600;
        transition: all 0.3s ease;
        background-color: var(--bg-input);
        color: var(--text-primary);
        border: 2px solid var(--border-color);
        text-align: center;
        cursor: pointer;
        font-size: 0.95rem;
        width: 80%;
        max-width: 300px;
        display: block;
    }

    .time-button:hover:not(.active) {
        background-color: var(--bg-input-focus);
        border-color: var(--accent-primary);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px var(--shadow-color);
    }

    .time-button.active {
        background-color: var(--accent-primary);
        border-color: var(--accent-hover);
        color: white;
        box-shadow: 0 4px 15px rgba(229, 9, 20, 0.4);
        transform: scale(1.02);
        pointer-events: none;
    }

    .day-header {
        width: 100%;
        text-align: center;
        font-size: 1rem;
        font-weight: bold;
        color: var(--text-primary);
        margin: 1.5rem 0 0.75rem;
        padding-top: 1.5rem;
        border-top: 1px solid var(--border-color);
    }

    .day-header:first-child {
        margin-top: 0;
        padding-top: 0;
        border-top: none;
    }

    /* Группы кнопок для каждого дня */
    .day-buttons-container {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        width: 100%;
        align-items: center;
    }

    #screening-details p {
        margin: 0.5rem 0;
        color: var(--text-secondary);
        font-size: 1rem;
    }

    #screening-details strong {
        color: var(--text-primary);
    }

    #base-price {
        color: var(--accent-primary);
        font-weight: bold;
        font-size: 1.2rem;
    }

    /* Основной контент с двумя колонками */
    .seats-layout {
        display: flex;
        flex-direction: column;
        gap: 2rem;
        max-width: 1200px;
        margin: 0 auto;
    }

    @media (min-width: 1024px) {
        .seats-layout {
            flex-direction: row;
        }
    }

    /* Левая колонка: схема мест */
    .seating-map-container {
        flex: 1;
        background-color: var(--bg-card);
        padding: 2rem;
        border-radius: 12px;
        box-shadow: 0 4px 20px var(--shadow-color);
        position: relative;
        transition: all 0.3s ease;
    }

    /* Индикатор экрана */
    .screen-indicator {
        width: 100%;
        height: 30px;
        background: linear-gradient(to right, var(--accent-primary), var(--accent-hover));
        color: white;
        text-align: center;
        line-height: 30px;
        font-size: 1rem;
        font-weight: bold;
        border-radius: 8px 8px 0 0;
        margin-bottom: 2rem;
        box-shadow: 0 4px 12px rgba(229, 9, 20, 0.3);
        letter-spacing: 0.2em;
        text-transform: uppercase;
    }

    /* Схема мест */
    .seating-map-wrapper {
        overflow-x: auto;
        padding: 1rem 0;
        margin-bottom: 2rem;
        -ms-overflow-style: none;
        scrollbar-width: none;
    }

    .seating-map-wrapper::-webkit-scrollbar {
        display: none;
    }

    #seating-map {
        margin: 0 auto;
        max-width: max-content;
    }

    .seat-row {
        display: flex;
        align-items: center;
        gap: 4px;
        justify-content: center;
        margin-bottom: 8px;
    }

    .row-label {
        width: 30px;
        min-width: 30px;
        line-height: 32px;
        text-align: right;
        font-weight: bold;
        color: var(--text-secondary);
        margin-right: 8px;
        font-size: 0.9rem;
    }

    .seat {
        width: 32px;
        height: 32px;
        background-color: var(--bg-input);
        border: 2px solid var(--border-color);
        border-radius: 6px;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.75rem;
        font-weight: 600;
        transition: all 0.2s ease-in-out;
        color: var(--text-primary);
        box-shadow: 0 2px 4px var(--shadow-color);
    }

    .seat.available:hover {
        background-color: var(--border-light);
        border-color: var(--accent-primary);
        transform: scale(1.1);
    }

    .seat.selected {
        background-color: var(--accent-primary);
        border-color: var(--accent-hover);
        color: white;
        transform: scale(1.1);
        box-shadow: 0 0 12px rgba(229, 9, 20, 0.5);
    }

    .seat.booked {
        background-color: var(--bg-primary);
        border-color: var(--border-color);
        color: var(--text-muted);
        cursor: not-allowed;
        opacity: 0.6;
        pointer-events: none;
    }

    /* Легенда мест */
    .seating-legend {
        display: flex;
        justify-content: center;
        gap: 1.5rem;
        flex-wrap: wrap;
        margin-top: 1rem;
    }

    .legend-item {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--text-secondary);
        font-size: 0.9rem;
    }

    .legend-seat {
        width: 20px;
        height: 20px;
        border-radius: 4px;
        border: 2px solid transparent;
    }

    .legend-available {
        background-color: var(--bg-input);
        border-color: var(--border-color);
    }

    .legend-selected {
        background-color: var(--accent-primary);
        border-color: var(--accent-hover);
    }

    .legend-booked {
        background-color: var(--bg-primary);
        border-color: var(--border-color);
        opacity: 0.6;
    }

    /* Правая колонка: панель бронирования */
    .booking-summary-panel {
        width: 100%;
        background-color: var(--bg-card);
        padding: 2rem;
        border-radius: 12px;
        box-shadow: 0 4px 20px var(--shadow-color);
        transition: all 0.3s ease;
    }

    @media (min-width: 1024px) {
        .booking-summary-panel {
            width: 350px;
            position: sticky;
            top: 2rem;
            align-self: flex-start;
        }
    }

    .booking-summary-panel h2 {
        font-size: 1.5rem;
        color: var(--text-primary);
        margin-bottom: 1.5rem;
        padding-bottom: 0.75rem;
        border-bottom: 2px solid var(--border-color);
        font-weight: bold;
    }

    .summary-details {
        margin-bottom: 1.5rem;
    }

    .summary-details p {
        margin: 0.75rem 0;
        color: var(--text-secondary);
        font-size: 1rem;
    }

    .summary-details span {
        font-weight: bold;
        color: var(--accent-primary);
        font-size: 1.3rem;
    }

    #selected-count {
        color: var(--text-primary);
    }

    /* Список выбранных мест */
    #selected-seats-list {
        margin: 1.5rem 0;
        padding: 1rem;
        background-color: var(--bg-primary);
        border-radius: 8px;
        min-height: 60px;
        border: 1px dashed var(--border-color);
    }

    #selected-seats-list span {
        display: inline-block;
        background-color: var(--bg-input);
        color: var(--text-primary);
        padding: 6px 12px;
        border-radius: 6px;
        margin: 4px;
        font-size: 0.9rem;
        font-weight: 500;
    }

    #selected-seats-list .placeholder-text {
        color: var(--text-muted);
        font-style: italic;
        text-align: center;
        display: block;
        width: 100%;
    }

    /* Кнопка оплаты */
    #book-button {
        width: 100%;
        padding: 1rem;
        background-color: var(--accent-primary);
        color: white;
        border: none;
        border-radius: 8px;
        font-size: 1.1rem;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.3s ease;
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 10px;
    }

    #book-button:hover:not(:disabled) {
        background-color: var(--accent-hover);
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(229, 9, 20, 0.3);
    }

    #book-button:active:not(:disabled) {
        transform: translateY(0);
    }

    #book-button:disabled {
        background-color: var(--border-color);
        cursor: not-allowed;
        opacity: 0.7;
        transform: none;
        box-shadow: none;
    }

    /* Сообщения */
    #booking-message {
        margin-top: 1rem;
        padding: 0.75rem;
        border-radius: 6px;
        font-size: 0.9rem;
        text-align: center;
        font-weight: 500;
        transition: all 0.3s ease;
    }

    .booking-message.text-text-muted {
        color: var(--text-muted);
    }

    .booking-message.text-red-500 {
        color: var(--error-color);
        background-color: rgba(220, 53, 69, 0.1);
        border: 1px solid rgba(220, 53, 69, 0.3);
    }

    .booking-message.text-green-500 {
        color: var(--success-color);
        background-color: rgba(40, 167, 69, 0.1);
        border: 1px solid rgba(40, 167, 69, 0.3);
    }

    /* Индикаторы загрузки */
    .loading-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-color: rgba(0, 0, 0, 0.7);
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 10;
        border-radius: 12px;
    }

    .light-theme .loading-overlay {
        background-color: rgba(255, 255, 255, 0.9);
    }

    .spinner {
        border: 4px solid rgba(255, 255, 255, 0.3);
        border-top: 4px solid var(--accent-primary);
        border-radius: 50%;
        width: 40px;
        height: 40px;
        animation: spin 1s linear infinite;
    }

    .light-theme .spinner {
        border: 4px solid rgba(0, 0, 0, 0.1);
        border-top: 4px solid var(--accent-primary);
    }

    @keyframes spin {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
    }

    /* Пустое состояние */
    .empty-state {
        text-align: center;
        padding: 3rem 1rem;
        color: var(--text-muted);
    }

    .empty-state i {
        font-size: 48px;
        margin-bottom: 1rem;
        opacity: 0.5;
    }

    /* Адаптивность */
    @media (max-width: 768px) {
        .seat-selection-page {
            padding: 1rem;
        }

        #movie-title {
            font-size: 2rem;
        }

        .time-selection-container {
            padding: 1rem;
        }

        .time-button {
            padding: 0.6rem 0.8rem;
            font-size: 0.9rem;
        }

        .seating-map-container,
        .booking-summary-panel {
            padding: 1.5rem;
        }

        .seat {
            width: 28px;
            height: 28px;
            font-size: 0.7rem;
        }

        .row-label {
            width: 25px;
            min-width: 25px;
            font-size: 0.8rem;
        }

        .seating-legend {
            gap: 1rem;
            font-size: 0.8rem;
        }

        .legend-seat {
            width: 16px;
            height: 16px;
        }
    }

    @media (max-width: 480px) {
        #movie-title {
            font-size: 1.75rem;
        }

        .time-button-group {
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 0.5rem;
        }

        .day-header {
            font-size: 0.9rem;
        }

        .seating-map-container,
        .booking-summary-panel {
            padding: 1rem;
        }

        .seat {
            width: 24px;
            height: 24px;
            font-size: 0.65rem;
        }

        .row-label {
            width: 20px;
            min-width: 20px;
            font-size: 0.75rem;
            margin-right: 6px;
        }

        .seating-legend {
            flex-direction: column;
            align-items: flex-start;
            gap: 0.5rem;
        }

        #book-button {
            padding: 0.875rem;
            font-size: 1rem;
        }
    }

    /* ======================== */
    /* СВЕТЛАЯ ТЕМА ДЛЯ СТРАНИЦЫ */
    /* ======================== */
    .light-theme .seat-selection-page {
        background-color: #f8fafc;
    }

    .light-theme #movie-title {
        color: #1e293b;
    }

    .light-theme .back-link {
        color: #dc2626;
    }

    .light-theme .back-link:hover {
        color: #b91c1c;
    }

    .light-theme .time-selection-container {
        background-color: white;
        border: 1px solid #e2e8f0;
    }

    .light-theme .time-selection-container h2 {
        color: #334155;
    }

    .light-theme .time-button {
        background-color: white;
        border-color: #cbd5e1;
        color: #475569;
    }

    .light-theme .time-button:hover:not(.active) {
        background-color: #f1f5f9;
        border-color: #dc2626;
        color: #1e293b;
    }

    .light-theme .time-button.active {
        background-color: #dc2626;
        border-color: #b91c1c;
        color: white;
        box-shadow: 0 4px 6px -1px rgba(220, 38, 38, 0.3);
    }

    .light-theme .day-header {
        color: #475569;
        border-top-color: #e2e8f0;
    }

    /* Детали сеанса в светлой теме */
    .light-theme #screening-details {
        background-color: white;
        border-left-color: #dc2626;
        border: 1px solid #e2e8f0;
    }

    .light-theme #screening-details p {
        color: #64748b;
    }

    .light-theme #screening-details strong {
        color: #1e293b;
    }

    .light-theme #base-price {
        color: #dc2626;
    }

    /* Контейнер схемы мест в светлой теме */
    .light-theme .seating-map-container {
        background-color: white;
        border: 1px solid #e2e8f0;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    }

    .light-theme .screen-indicator {
        background: linear-gradient(to right, #dc2626, #b91c1c);
        box-shadow: 0 2px 4px rgba(220, 38, 38, 0.2);
    }

    .light-theme .seat {
        background-color: #f8fafc;
        border-color: #cbd5e1;
        color: #475569;
        box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
    }

    .light-theme .seat.available:hover {
        background-color: #e2e8f0;
        border-color: #dc2626;
    }

    .light-theme .seat.selected {
        background-color: #dc2626;
        border-color: #b91c1c;
        box-shadow: 0 0 8px rgba(220, 38, 38, 0.4);
    }

    .light-theme .seat.booked {
        background-color: #f1f5f9;
        border-color: #cbd5e1;
        color: #94a3b8;
    }

    .light-theme .row-label {
        color: #64748b;
    }

    /* Легенда в светлой теме */
    .light-theme .legend-item {
        color: #64748b;
    }

    .light-theme .legend-available {
        background-color: #f8fafc;
        border-color: #cbd5e1;
    }

    .light-theme .legend-selected {
        background-color: #dc2626;
        border-color: #b91c1c;
    }

    .light-theme .legend-booked {
        background-color: #f1f5f9;
        border-color: #cbd5e1;
    }

    .light-theme .seating-map-container,
    .light-theme .booking-summary-panel {
        background-color: var(--bg-card) !important;
    }

    .light-theme .booking-summary-panel h2 {
        color: #1e293b;
        border-bottom-color: #e2e8f0;
    }

    .light-theme .summary-details p {
        color: #64748b;
    }

    .light-theme .summary-details span {
        color: #dc2626;
    }

    .light-theme #selected-count {
        color: #1e293b;
    }

    .light-theme #selected-seats-list {
        background-color: #f8fafc;
        border-color: #e2e8f0;
    }

    .light-theme #selected-seats-list span {
        background-color: white;
        border: 1px solid #e2e8f0;
        color: #475569;
    }

    .light-theme #selected-seats-list .placeholder-text {
        color: #94a3b8;
    }

    .light-theme #book-button:disabled {
        background-color: #cbd5e1;
        color: #94a3b8;
    }

    /* Сообщения в светлой теме */
    .light-theme .booking-message.text-text-muted {
        color: #94a3b8;
    }

    .light-theme .booking-message.text-red-500 {
        color: #dc2626;
        background-color: #fee2e2;
        border-color: #fecaca;
    }

    .light-theme .booking-message.text-green-500 {
        color: #059669;
        background-color: #d1fae5;
        border-color: #a7f3d0;
    }

    .reservation-timer {
        margin-top: 10px;
        padding: 10px;
        background-color: #fff3cd;
        border: 1px solid #ffeaa7;
        border-radius: 6px;
        text-align: center;
    }

    .light-theme .reservation-timer {
        background-color: #fff3cd;
        border-color: #ffeaa7;
    }

    .reservation-timer .text-yellow-600 {
        color: #856404;
    }

    .reservation-timer .text-red-600 {
        color: #721c24;
    }
</style>


<div class="seat-selection-page page-container main-wrapper">
    <div class="text-center mb-10">
        <a id="back-link" href="#" class="back-link inline-block mb-4">
            &larr; Назад к фильму (Смотреть все сеансы)
        </a>
        <h1 id="movie-title" class="text-3xl font-extrabold">Загрузка...</h1>

        <div class="time-selection-container">
            <div id="time-loading-overlay" class="loading-overlay">
                <div class="spinner"></div>
            </div>

            <h2>Выберите время сеанса:</h2>
            <div id="time-selection-buttons" class="time-button-group">
            </div>
        </div>

        <div id="screening-details">
            <p><strong>Зал:</strong> <span id="hall-name">—</span></p>
            <p><strong>Время:</strong> <span id="screening-time">—</span></p>
            <p><strong>Цена:</strong>
                <span id="base-price">0.00</span> BYN за место
            </p>
        </div>
    </div>

    <div class="seats-layout">
        <div class="seating-map-container">
            <div id="seating-loading-overlay" class="loading-overlay">
                <div class="spinner"></div>
            </div>
            <div class="screen-indicator">ЭКРАН</div>
            <div class="seating-map-wrapper">
                <div id="seating-map" class="seating-map">
                    <div class="text-center pt-8">Выберите время сеанса для загрузки схемы зала.</div>
                </div>
            </div>

            <div class="seating-legend">
                <div class="legend-item">
                    <div class="legend-seat legend-available"></div>
                    Свободно
                </div>
                <div class="legend-item">
                    <div class="legend-seat legend-selected"></div>
                    Выбрано
                </div>
                <div class="legend-item">
                    <div class="legend-seat legend-booked"></div>
                    Занято
                </div>
            </div>
        </div>

        <div class="booking-summary-panel">
            <h2>Ваш заказ</h2>
            <div class="summary-details">
                <p>Выбрано мест: <span id="selected-count">0</span></p>
                <p>Сумма к оплате: <span id="total-price">0.00</span> BYN</p>
            </div>

            <div id="selected-seats-list">
                <span class="placeholder-text">Места не выбраны</span>
            </div>

            <button id="book-button" disabled>
                Оплатить
            </button>

            <div id="booking-message" class="booking-message"></div>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', initApp);
    const API_BASE = '/movies/api';
    let initialDataJson;
    let movieId;

    try {

        initialDataJson = JSON.parse(`{{{initialData}}}`);
        movieId = `{{{movieId}}}`;
    } catch (e) {
        console.error("КРИТИЧЕСКАЯ ОШИБКА ПАРСИНГА initialData. Вероятно, сервер не передал валидный JSON:", e);
        initialDataJson = { screenings: [], initialSeatData: null };
        movieId = '';
    }
    let allScreenings = initialDataJson.screenings || [];
    // currentScreeningDetails содержит данные текущего активного сеанса (обновляется при AJAX)
    let currentScreeningDetails = initialDataJson.initialSeatData || null;
    let selectedSeats = [];

    // *** ДОБАВЛЕНО ДЛЯ ОТЛАДКИ (Проверьте эти логи в консоли!) ***
    console.log("Статус загрузки сеансов:", allScreenings.length > 0 ? "Сеансы загружены." : "Сеансы НЕ загружены (allScreenings пуст).");
    console.log("Данные сеансов (allScreenings):", allScreenings);

    const titleElement = document.getElementById('movie-title');
    const timeButtonsContainer = document.getElementById('time-selection-buttons');
    const timeLoadingOverlay = document.getElementById('time-loading-overlay'); // <-- ДОБАВЛЕНО
    const seatingLoadingOverlay = document.getElementById('seating-loading-overlay');
    const hallNameSpan = document.getElementById('hall-name');
    const screeningTimeSpan = document.getElementById('screening-time');
    const basePriceSpan = document.getElementById('base-price');
    const seatingMap = document.getElementById('seating-map');
    const selectedCountSpan = document.getElementById('selected-count');
    const totalPriceSpan = document.getElementById('total-price');
    const bookButton = document.getElementById('book-button');
    const bookingMessageDiv = document.getElementById('booking-message');
    const selectedSeatsListDiv = document.getElementById('selected-seats-list');
    const backLink = document.getElementById('back-link');

    // =========================================================
    // 4. ФУНКЦИИ РЕНДЕРИНГА И ЛОГИКИ ВЫБОРА МЕСТ
    // =========================================================

    function updateScreeningDetails() {
        if (!currentScreeningDetails) {
            hallNameSpan.textContent = '—';
            screeningTimeSpan.textContent = '—';
            basePriceSpan.textContent = '0.00';
            return;
        }
        // startTime содержит форматированную полную дату (ДД.ММ. ЧЧ:ММ)
        screeningTimeSpan.textContent = currentScreeningDetails.startTime;
        hallNameSpan.textContent = currentScreeningDetails.hall;
        basePriceSpan.textContent = currentScreeningDetails.basePrice ? currentScreeningDetails.basePrice.toFixed(2) : '0.00';
    }

    function updateSummary() {
        const count = selectedSeats.length;
        const price = currentScreeningDetails ? currentScreeningDetails.basePrice : 0;
        const total = (count * price);
        const totalDisplay = total.toFixed(2);

        selectedCountSpan.textContent = count;
        totalPriceSpan.textContent = totalDisplay;

        selectedSeats.sort((a, b) => {
            const [rowA, seatA] = a.split('-').map(Number);
            const [rowB, seatB] = b.split('-').map(Number);
            if (rowA !== rowB) return rowA - rowB;
            return seatA - seatB;
        });

        bookButton.disabled = count === 0 || !currentScreeningDetails;
        bookingMessageDiv.textContent = '';

        if (count === 0) {
            selectedSeatsListDiv.innerHTML = '<span class="text-sm">Места не выбраны</span>';
        } else {
            selectedSeatsListDiv.innerHTML = selectedSeats.map(key => {
                const [row, seat] = key.split('-');
                return `<span>Ряд ${row}, Место ${seat}</span>`;
            }).join('');
        }
    }

    function renderSeatingMap() {
        if (!seatingMap) {
            console.error('Критическая ошибка: Элемент #seating-map не найден в DOM.');
            return;
        }

        if (!currentScreeningDetails) {
            seatingMap.innerHTML = '<div class="text-center text-text-muted pt-8">Выберите время сеанса.</div>';
            return;
        }

        const { rowsCount, seatsPerRow, bookedSeatKeys } = currentScreeningDetails;

        if (rowsCount <= 0 || seatsPerRow <= 0) {
            seatingMap.innerHTML = '<div class="text-center text-text-muted mt-4">Ошибка: Некорректные размеры зала.</div>';
            return;
        }

        seatingMap.innerHTML = '';
        const fragment = document.createDocumentFragment();

        for (let row = 1; row <= rowsCount; row++) {
            const rowDiv = document.createElement('div');
            rowDiv.className = 'seat-row';

            const rowLabel = document.createElement('div');
            rowLabel.className = 'row-label';
            rowLabel.textContent = row;
            rowDiv.appendChild(rowLabel);

            for (let seatNum = 1; seatNum <= seatsPerRow; seatNum++) {
                const seatKey = `${row}-${seatNum}`;
                const seatDiv = document.createElement('div');
                seatDiv.className = 'seat';
                seatDiv.textContent = seatNum;
                seatDiv.dataset.key = seatKey;

                if (bookedSeatKeys.includes(seatKey)) {
                    seatDiv.classList.add('booked');
                } else {
                    seatDiv.classList.add('available');
                    seatDiv.addEventListener('click', toggleSeatSelection);
                }

                if (selectedSeats.includes(seatKey)) {
                    seatDiv.classList.add('selected');
                }

                rowDiv.appendChild(seatDiv);
            }
            fragment.appendChild(rowDiv);
        }
        seatingMap.appendChild(fragment);
        updateSummary();
    }

    function toggleSeatSelection(event) {
        const seatDiv = event.target.closest('.seat');
        if (!seatDiv || seatDiv.classList.contains('booked') || !currentScreeningDetails) return;

        const seatKey = seatDiv.dataset.key;

        if (seatDiv.classList.contains('selected')) {
            seatDiv.classList.remove('selected');
            selectedSeats = selectedSeats.filter(key => key !== seatKey);
        } else {
            seatDiv.classList.add('selected');
            selectedSeats.push(seatKey);
        }

        updateSummary();
    }


    async function loadScreening(screeningId) {
        if (!screeningId) return;

        document.querySelectorAll('.time-button').forEach(btn => {
            btn.classList.remove('active');
        });
        const activeBtn = document.querySelector(`.time-button[data-id="${screeningId}"]`);
        if (activeBtn) activeBtn.classList.add('active');

        seatingLoadingOverlay.style.display = 'flex';
        seatingMap.innerHTML = '<div class="text-center text-text-muted pt-8">Загрузка схемы зала...</div>';
        bookButton.disabled = true;
        selectedSeats = []; // Сбрасываем выбранные места

        try {
            const response = await fetch(`${API_BASE}/seats/${screeningId}`);
            if (!response.ok) {
                const errorData = await response.json();
                throw new Error(errorData.error || `Ошибка HTTP: ${response.status}`);
            }
            const data = await response.json();

            currentScreeningDetails = data;

            updateScreeningDetails();
            renderSeatingMap();

        } catch (error) {
            console.error('Ошибка загрузки схемы мест:', error);
            seatingMap.innerHTML = `<div class="text-center text-red-500 pt-8">Не удалось загрузить схему: ${error.message}</div>`;
            currentScreeningDetails = null;
            updateScreeningDetails();
        } finally {
            seatingLoadingOverlay.style.display = 'none';
            updateSummary();
        }
    }

    function renderTimeButtons() {
        timeButtonsContainer.innerHTML = ''; // Очищаем контейнер

        if (allScreenings.length === 0) {
            timeButtonsContainer.innerHTML = '<p class="text-red-500 font-bold text-center">Нет доступных сеансов.</p>';
            return;
        }

        const initialId = currentScreeningDetails ? currentScreeningDetails.id : null;
        const fragment = document.createDocumentFragment();

        allScreenings.forEach(dayGroup => {
            // Заголовок дня
            const header = document.createElement('h3');
            header.className = 'day-header';
            header.textContent = dayGroup.dateLabel + ':';
            fragment.appendChild(header);

            // Контейнер кнопок для этого дня
            const dayButtonsContainer = document.createElement('div');
            dayButtonsContainer.className = 'day-buttons-container';

            dayGroup.screenings.forEach(scr => {
                const button = document.createElement('button');
                button.className = 'time-button';
                button.textContent = `${scr.startTime} (${scr.hall})`;
                button.dataset.id = scr.id;
                button.dataset.fullTime = scr.fullDisplayTime;

                if (scr.id === initialId) {
                    button.classList.add('active');
                }

                button.addEventListener('click', () => loadScreening(scr.id));
                dayButtonsContainer.appendChild(button);
            });

            fragment.appendChild(dayButtonsContainer);
        });

        timeButtonsContainer.appendChild(fragment);
    }


    /** Инициализация приложения. */
    async function initApp() {
        seatingLoadingOverlay.style.display = 'none';

        // 1. Установка заголовка
        titleElement.textContent = currentScreeningDetails?.movieTitle || 'Выбор мест';

        // 2. Установка ссылки "Назад"
        backLink.href = `/movies/${movieId}`;

        // 3. Рендеринг кнопок времени (с группировкой)
        renderTimeButtons();

        // ИЗМЕНЕНИЕ 5: Скрываем оверлей загрузки времени после рендеринга кнопок
        if (timeLoadingOverlay) {
            timeLoadingOverlay.style.display = 'none';
        }

        // 4. Рендеринг схемы мест и деталей для первого сеанса (если данные есть)
        if (currentScreeningDetails) {
            updateScreeningDetails();
            renderSeatingMap();
            // Показываем, что первый сеанс активен
            const initialButton = document.querySelector(`.time-button[data-id="${currentScreeningDetails.id}"]`);
            if (initialButton) {
                initialButton.classList.add('active');
            }
        } else {
            // Если initialSeatData пуст, но сеансы есть, предлагаем выбрать
            if (allScreenings.length > 0) {
                seatingMap.innerHTML = '<div class="text-center text-text-muted pt-8">Выберите время сеанса для загрузки схемы зала.</div>';
            } else {
                seatingMap.innerHTML = '<div class="text-center text-red-500 pt-8">Нет доступных сеансов.</div>';
            }
            updateScreeningDetails();
            bookButton.disabled = true;
        }
    }


    // =========================================================
    // 6. ФУНКЦИЯ ОПЛАТЫ ЮKASSA (ВОССТАНОВЛЕНА)
    // =========================================================

    async function handleBooking() {
        if (!currentScreeningDetails || selectedSeats.length === 0 || bookButton.disabled) return;

        const totalAmount = (selectedSeats.length * currentScreeningDetails.basePrice);

        if (totalAmount <= 0) {
            bookingMessageDiv.textContent = 'Ошибка: Сумма к оплате должна быть больше 0.';
            bookingMessageDiv.classList.add('text-red-500');
            return;
        }

        bookButton.disabled = true;
        bookButton.textContent = 'Бронирование...';
        bookingMessageDiv.textContent = 'Резервирование мест...';
        bookingMessageDiv.className = 'booking-message text-text-muted';

        try {
            // 1. Сначала резервируем места на 10 минут
            const reserveResponse = await fetch('/movies/api/reserve-seats', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    screeningId: currentScreeningDetails.id,
                    seatKeys: selectedSeats
                }),
            });

            if (!reserveResponse.ok) {
                const errorData = await reserveResponse.json();

                if (reserveResponse.status === 409) {
                    // Места уже заняты - обновляем схему
                    await loadScreening(currentScreeningDetails.id);
                    bookingMessageDiv.textContent = 'Некоторые места уже заняты. Пожалуйста, выберите другие места.';
                    bookingMessageDiv.classList.add('text-red-500');
                    bookButton.disabled = false;
                    bookButton.textContent = 'Оплатить';
                    return;
                }

                throw new Error(errorData.error || 'Ошибка бронирования');
            }

            const reserveResult = await reserveResponse.json();

            // 2. После успешного бронирования создаем платеж
            bookingMessageDiv.textContent = 'Создание платежа...';

            const temporaryTicketIds = selectedSeats.map(key => ({
                screeningId: currentScreeningDetails.id,
                seatKey: key,
                price: currentScreeningDetails.basePrice,
            }));

            const orderDescription = `Билеты на фильм "${currentScreeningDetails.movieTitle}" (${selectedSeats.length} шт.)`;

            const paymentResponse = await fetch('/payment/place-order', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    totalAmount: totalAmount.toFixed(2),
                    orderDescription: orderDescription,
                    ticketIds: temporaryTicketIds,
                    reservationId: reserveResult.reservationId
                }),
            });

            const paymentResult = await paymentResponse.json();

            if (paymentResponse.ok) {
                const redirectUrl = paymentResult.url;

                bookingMessageDiv.textContent = 'Перенаправление на страницу оплаты...';
                bookingMessageDiv.classList.remove('text-text-muted', 'text-red-500');
                bookingMessageDiv.classList.add('text-green-500');

                setTimeout(() => {
                    window.location.href = redirectUrl;
                }, 500);

            } else {
                // В случае ошибки оплаты, нужно освободить забронированные места
                if (reserveResult.reservationId) {
                    // Можно добавить endpoint для отмены бронирования
                    // await fetch(`/movies/api/cancel-reservation/${reserveResult.reservationId}`, { method: 'DELETE' });
                }

                if (paymentResponse.status === 401) {
                    const redirectUrl = paymentResult.redirect || '/auth/login';
                    bookingMessageDiv.textContent = paymentResult.error || 'Необходимо войти в систему.';
                    bookingMessageDiv.classList.add('text-red-500');
                    bookButton.textContent = 'Авторизация...';

                    setTimeout(() => {
                        window.location.href = redirectUrl;
                    }, 1000);
                    return;
                }

                bookingMessageDiv.textContent = paymentResult.error || 'Ошибка при создании платежа.';
                bookingMessageDiv.classList.add('text-red-500');
                bookButton.disabled = false;
                bookButton.textContent = 'Оплатить';
            }

        } catch (error) {
            console.error('Ошибка при бронировании/оплате:', error);
            bookingMessageDiv.textContent = 'Ошибка сети. Пожалуйста, попробуйте еще раз.';
            bookingMessageDiv.classList.add('text-red-500');
            bookButton.disabled = false;
            bookButton.textContent = 'Оплатить';
        }
    }

    function startReservationTimer(minutes) {
        let timeLeft = minutes * 60;
        const timerElement = document.createElement('div');
        timerElement.id = 'reservation-timer';
        timerElement.className = 'reservation-timer';
        document.querySelector('.booking-summary-panel').appendChild(timerElement);

        const timer = setInterval(() => {
            timeLeft--;
            const mins = Math.floor(timeLeft / 60);
            const secs = timeLeft % 60;
            timerElement.textContent = `Осталось времени: ${mins}:${secs < 10 ? '0' : ''}${secs}`;

            if (timeLeft <= 0) {
                clearInterval(timer);
                timerElement.textContent = 'Время бронирования истекло';
                timerElement.classList.add('expired');
                // Можно автоматически обновить схему мест
                setTimeout(() => loadScreening(currentScreeningDetails.id), 2000);
            }
        }, 1000);
    }

    bookButton.addEventListener('click', handleBooking);
</script>
